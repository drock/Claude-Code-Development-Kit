# Test Project

A sample project for testing CDK plugins. This simulates a real target project
where CDK plugins would be installed.

## Project Structure
- `src/` — Application source code
- `docs/ai-context/` — AI context documentation (created by cdk-docs:scaffold)
- `.claude/` — Claude Code configuration directory
